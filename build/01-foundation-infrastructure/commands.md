# Terraform Commands Used

The following Terraform commands were used during the implementation of the foundation infrastructure.

```bash
# Initialise the Terraform working directory
terraform init

# Validate the Terraform configuration
terraform validate

# Format the Terraform code
terraform fmt

# Preview the infrastructure changes
terraform plan

# Deploy the infrastructure
terraform apply

# Display resource outputs
terraform output

# Remove all deployed resources
terraform destroy
```
